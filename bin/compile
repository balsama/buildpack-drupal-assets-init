#!/usr/bin/env bash
# Initializes the app with dropzone.

set -ev

# Configure directories
build_dir=$1
cache_dir=$2
env_dir=$3

cd $build_dir

./assets init

./assets fetch bower-asset/dropzone
./assets process bower-asset/dropzone
./assets rehash bower-asset/dropzone

chmod -R 744 www
